# Builder Constraint Contract

The active toolbox contract lives at the repository root:

[`CONTRACT.md`](../CONTRACT.md)

Agents should read and honor that contract before beginning meaningful project
work. This pointer exists so fresh-project setup doctrine can refer to a stable
`_docs/builder_constraint_contract.md` surface while the root contract remains
the canonical source.
