# _AppJOURNAL

This folder is runtime-generated working state for the source package.

The vendored authority kit should not ship source-project-specific journal
config or manifest files here. Those files are generated on demand when the
package is initialized or the UI/bootstrap path is run.
